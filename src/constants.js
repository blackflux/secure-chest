export const GZIP_MODE = Object.freeze({
  AUTO: 'AUTO',
  FORCE: 'FORCE',
  NEVER: 'NEVER'
});

export const ENCODING = Object.freeze({
  utf8: 'utf8',
  ascii: 'ascii',
  latin1: 'latin1',
  binary: 'binary'
});
